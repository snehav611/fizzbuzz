$(document).ready(function() {
	/*Setting variables*/	
	var count = 1;
	var fizzbuzz for (count = 1; count <= 100; count++) {
	    if (count % 15 == 0) {
	       $('ul').append('<li>' + 'Fizz Buzz' + '</li>');
	    } else if (count % 5 == 0) {
	       $('ul').append('<li>' + 'Buzz' + '</li>')
	    } else if (count % 3 == 0) {
	       $('ul').append('<li>' + 'Fizz' + '</li>')
	    } else {
	       $('ul').append('<li>' + count + '</li>')
	    }
	};
	/*Click button*/
	
});

